# В dev API-запросы идут относительными путями через vite-прокси (vite.config.ts):
# он переписывает /api → /api/v1 на локальном бэкенде (:8000).
# Прямой URL вида http://localhost:8000 ломает пути (бэк ждёт /api/v1) — оставляем пустым.
VITE_API_BASE_URL=
