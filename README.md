# ⚡ Take Smart

> Современный интернет-магазин электроники с профессиональным дизайном

Полнофункциональный e-commerce сайт для продажи смартфонов, ноутбуков, планшетов и аксессуаров. Реализован на современном стеке с React, FastAPI и PostgreSQL.

![Take Smart](https://img.shields.io/badge/Made%20with-React%20%2B%20FastAPI-yellow?style=for-the-badge)
![License](https://img.shields.io/badge/License-MIT-green?style=for-the-badge)

## 🚀 Возможности

- 📱 **Адаптивный дизайн** — идеально работает на всех устройствах (mobile, tablet, desktop)
- 🎨 **Современный UI** — основан на дизайне repremium.ru с желтыми акцентами
- 🛍️ **Каталог товаров** — с фильтрами по категориям, брендам, цене, поиском и сортировкой
- 📦 **Карточки товаров** — детальная информация, характеристики, галерея, отзывы
- 📝 **Форма заказа** — быстрое оформление с валидацией данных
- 👨‍💼 **Админ-панель** — управление заявками, статистика, быстрые действия
- 🔔 **Cookie consent** — всплывающее уведомление о cookies
- 💬 **WhatsApp виджет** — быстрая связь с поддержкой
- 🏷️ **Система бейджей** — "Хит продаж", "Новинка", "Скидка"
- 🔍 **SEO-оптимизация** — мета-теги, Open Graph, структурированные данные

## 🛠 Технологический стек

### Frontend
- **React 18.3** — современная библиотека для UI
- **TypeScript** — типизация для надежности кода
- **Vite 7.3** — молниеносная сборка
- **Tailwind CSS 3.4** — utility-first стилизация
- **React Router 7.1** — клиентская маршрутизация

### Backend
- **Python 3.13** — современная версия Python
- **FastAPI 0.115** — производительный веб-фреймворк
- **SQLAlchemy 2.0** — ORM для работы с БД
- **PostgreSQL 16** — надежная реляционная БД
- **Alembic** — система миграций
- **Pydantic** — валидация данных

### DevOps
- **Docker** + **Docker Compose** — контейнеризация
- **Uvicorn** — ASGI сервер
- **Vite Dev Server** — горячая перезагрузка

## 📁 Структура проекта

```
take-smart/
├── frontend/                 # React приложение
│   ├── src/
│   │   ├── components/       # Переиспользуемые компоненты
│   │   │   ├── ui/           # UI kit (Button, Layout, Icons)
│   │   │   ├── ProductCard.tsx
│   │   │   └── Shell.tsx     # Главный layout (header, footer)
│   │   ├── pages/            # Страницы приложения
│   │   │   ├── HomePage.tsx
│   │   │   ├── CatalogPage.tsx
│   │   │   ├── ProductPage.tsx
│   │   │   ├── CartPage.tsx
│   │   │   └── AdminPage.tsx
│   │   ├── data/             # Моковые данные
│   │   │   └── products.ts   # Товары, категории, бренды
│   │   ├── lib/              # Утилиты
│   │   │   ├── config.ts
│   │   │   └── http.ts       # HTTP клиент
│   │   ├── App.tsx           # Главный компонент
│   │   └── main.tsx          # Точка входа
│   ├── package.json
│   └── vite.config.ts
│
├── backend/                  # FastAPI приложение
│   ├── app/
│   │   ├── main.py           # FastAPI app, routes
│   │   ├── models.py         # SQLAlchemy модели
│   │   ├── schemas.py        # Pydantic схемы
│   │   ├── db.py             # Database connection
│   │   └── settings.py       # Конфигурация
│   ├── alembic/              # Миграции БД
│   ├── requirements.txt
│   └── Dockerfile
│
├── docker-compose.yml        # Оркестрация сервисов
├── .env.example              # Пример переменных окружения
└── README.md                 # Документация
```

## 🚀 Быстрый старт

### С использованием Docker (рекомендуется)

1. **Склонируйте репозиторий**
   ```bash
   git clone https://github.com/yourusername/take-smart.git
   cd take-smart
   ```

2. **Настройте переменные окружения**
   ```bash
   cp .env.example .env
   ```

3. **Запустите все сервисы**
   ```bash
   docker compose up --build
   ```

4. **Откройте в браузере**
   - 🌐 Frontend: http://localhost:5173
   - ⚙️ Backend API: http://localhost:8000
   - 📚 API Docs: http://localhost:8000/docs

### Локальная разработка (без Docker)

#### Backend

1. **Установите PostgreSQL 16**
   - macOS: `brew install postgresql@16`
   - Ubuntu: `sudo apt install postgresql-16`
   - Windows: [Скачать с официального сайта](https://www.postgresql.org/download/)

2. **Создайте базу данных**
   ```bash
   psql postgres -c "CREATE ROLE take_smart WITH LOGIN PASSWORD 'take_smart_password';"
   psql postgres -c "CREATE DATABASE take_smart OWNER take_smart;"
   ```

3. **Настройте backend**
   ```bash
   cd backend
   cp .env.example .env
   
   # Создайте виртуальное окружение
   python3 -m venv venv
   source venv/bin/activate  # Windows: venv\Scripts\activate
   
   # Установите зависимости
   pip install -r requirements.txt
   
   # Запустите сервер
   uvicorn app.main:app --reload
   ```
   
   Backend доступен на http://localhost:8000

#### Frontend

1. **Установите Node.js 20+**
   - Скачайте с [nodejs.org](https://nodejs.org/)
   - Или используйте `nvm`: `nvm install 20`

2. **Настройте frontend**
   ```bash
   cd frontend
   
   # Установите зависимости
   npm install
   
   # Запустите dev-сервер
   npm run dev
   ```
   
   Frontend доступен на http://localhost:5173

## 📋 API Endpoints

### Orders (Заказы)

| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/api/orders` | Создать новый заказ |
| GET | `/api/orders` | Получить список всех заказов |
| DELETE | `/api/orders/{id}` | Удалить заказ |

#### POST /api/orders

**Request Body:**
```json
{
  "name": "Иван Иванов",
  "phone": "+7 (999) 123-45-67",
  "email": "ivan@example.com",
  "comment": "Доставка после 18:00",
  "product_id": "iphone-15-pro",
  "quantity": 1
}
```

**Response (201):**
```json
{
  "id": 1,
  "name": "Иван Иванов",
  "phone": "+7 (999) 123-45-67",
  "email": "ivan@example.com",
  "comment": "Доставка после 18:00",
  "product_id": "iphone-15-pro",
  "quantity": 1,
  "created_at": "2026-02-10T12:30:00Z"
}
```

### Health Check

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/health` | Проверка здоровья сервиса |

## 🎨 Дизайн и компоненты

### UI Kit

Проект включает набор переиспользуемых компонентов:

- **Button** — кнопки с вариантами (primary, secondary, outline, ghost)
- **Container** — контейнер с максимальной шириной и padding
- **Section** — секция с вертикальными отступами
- **Badge** — бейджи для товаров
- **Icons** — 30+ SVG иконок

### Цветовая палитра

- **Primary (Yellow)**: `#FBBF24` — основной акцентный цвет
- **Secondary (Dark)**: `#1F2937` — темный цвет для текста/кнопок
- **Background**: `#F9FAFB` — светлый фон
- **White**: `#FFFFFF` — белый для карточек

### Типографика

- **Заголовки**: Inter, font-weight: 700-900
- **Основной текст**: Inter, font-weight: 400-500
- **Акценты**: Inter, font-weight: 600

## 📱 Страницы

### 1. Главная страница (Home)
- Hero секция с статистикой и showcase товара
- Сетка категорий с иконками
- Избранные товары (карусель)
- Блок преимуществ (доставка, гарантия, оплата)
- Логотипы брендов
- Блок новинок
- CTA секции
- Trust badges

### 2. Каталог (Catalog)
- Боковой фильтр (категории, бренды, цена)
- Поиск по названию
- Сортировка (цена, популярность, новизна)
- Переключение вида (сетка/список)
- Сетка товаров (ProductCard)
- Активные фильтры с возможностью удаления
- Мобильная модалка фильтров

### 3. Страница товара (Product)
- Breadcrumbs навигация
- Галерея изображений
- Информация о товаре (название, цена, описание)
- Счетчик количества
- Кнопки "Заказать" и "Купить в 1 клик"
- Табы (Описание, Характеристики, Отзывы)
- Блоки преимуществ (доставка, гарантия)
- Похожие товары
- CTA блок

### 4. Оформление заказа (Cart)
- Превью выбранного товара (если есть)
- Форма с валидацией (имя, телефон, email, комментарий)
- Боковая панель с преимуществами и контактами
- Success страница после отправки
- Кнопка возврата в каталог

### 5. Админ-панель (Admin)
- Статистика (всего заявок, сегодня, за неделю, с комментарием)
- Список заявок с сортировкой по дате
- Быстрые действия (позвонить, удалить)
- Кнопка обновления списка
- Empty state для пустого списка

## 🔧 Конфигурация

### Переменные окружения

#### Backend (.env)
```env
# Database
DATABASE_URL=postgresql://take_smart:take_smart_password@localhost:5432/take_smart

# App
DEBUG=True
```

#### Frontend (встроено в код)
```typescript
// src/lib/config.ts
export const API_BASE_URL = 'http://localhost:8000'
```

## 🧪 Тестирование

### Backend
```bash
cd backend
pytest
```

### Frontend
```bash
cd frontend
npm run test
```

### TypeScript проверка
```bash
cd frontend
npx tsc --noEmit
```

## 📦 Production Deploy

### Сборка Docker образов

```bash
# Backend
docker build -t take-smart-backend ./backend

# Frontend (production build)
cd frontend
npm run build
```

### Docker Compose (Production)

Для production добавьте Nginx в `docker-compose.yml`:

```yaml
services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./frontend/dist:/usr/share/nginx/html
    depends_on:
      - backend
```

